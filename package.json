{
	"name": "bindery",
	"version": "0.1.0",
	"private": true,
	"description": "Editable regions for hand-built themes.",
	"license": "GPL-2.0-or-later",
	"scripts": {
		"build": "wp-scripts build && node tools/copy-static.js",
		"start": "wp-scripts start",
		"lint:js": "wp-scripts lint-js src-js"
	},
	"devDependencies": {
		"@wordpress/scripts": "^30.0.0"
	},
	"overrides": {
		"ajv": "^8.17.1"
	}
}
